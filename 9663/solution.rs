use std::io;

fn main() {
    let mut input_str = String::new();
    io::stdin().read_line(&mut input_str).unwrap();
    let size:u64 = input_str.trim().parse().unwrap();

    let mut left_space = size;
    let mut left_queen = size;
    while (left==0) || (left_queen) {
        
    }
}