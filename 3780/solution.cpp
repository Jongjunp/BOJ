#include <iostream>
using namespace std;
const int MAX = 20001;

int find_op(*int arr, int x) {

}

void union_op(*int arr, int x, int y) {

}

int main() {
    int T, N;
    char op;
    scanf("%d", &T);
    for (int t=0; t<T; t++) {
        int center[MAX];
        int size[MAX];
        scanf("%d", &N);
        while (true) {
            scanf("%c", &op);
            if (c=='O') {
                break;
            }
            else if (c=='I') {

            }
            else {

            }
        }
    }
}