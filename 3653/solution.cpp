#include <iostream>
using namespace std;
const int MAX = 100000;
int seq[MAX] = {0};
int tree[4*MAX] = {0};
int main() {
    ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
    int T,n,m;
    cin >> T;
    for (int i=0; i<T; i++) {
        cin >> n >> m;
    }
}