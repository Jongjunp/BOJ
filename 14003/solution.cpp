#include <iostream>
#include <vector>
using namespace std;
const int MAX = 1000001;
int targ[MAX] = {0};
int answer[MAX] = {0};
int main() {
    int N,num;
    cin >> N;
    for (int i=0; i<N; i++) {
        cin >> num;
         
    }
}