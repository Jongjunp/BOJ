#include <iostream>
#include <vector>
#include <stack>
using namespace std;
const int MAX = 10001;
stack<int> 
int DFS() {

}

int main() {
    ios::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
    int V,E,from,to;
    cin >> V >> E;
    for (int i=0; i<E; i++) {
        cin >> from >> to;
    }
}