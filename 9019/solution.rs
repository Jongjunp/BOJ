use std::io;

fn main() {
    let mut testnum_str = String::new();
    io::stdin().read_line(&mut testnum_str).unwrap();
    let num_test:u32 = testnum_str.trim().parse().unwrap();
    for i in 1..num_test {
        let mut AB_str = String::new();
        io::stdin().read_line(&mut AB_str).unwrap();
        let 
    }
}